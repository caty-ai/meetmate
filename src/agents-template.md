<!-- meetmate-generated template=1 -->
regenerate with `meetmate init --force`.

Meetmate configuration uses the key names `SONIOX_API_KEY`,
`FISH_AUDIO_API_KEY`, `FISH_AUDIO_VOICE_ID`, `ATTENDEE_API_KEY`,
`JOIN_SHARED_TOKEN`, `WS_SHARED_TOKEN`, `LLM_PROVIDER`,
`OPENCLAW_GATEWAY_URL`, and `OPENCLAW_GATEWAY_TOKEN`.

SECURITY NOTICE FOR AI AGENTS: never print, log, or commit values from `.env`.
